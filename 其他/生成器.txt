生成器对象是由一个 generator function 返回的,并且它符合可迭代协议和迭代器协议。
 	next()
 	return()方法返回给定的值并结束生成器。
 	throw()
 	
function* gen() { 
  yield 1;
  yield 2;
  yield 3;
}

let g = gen(); 
// "Generator { }"